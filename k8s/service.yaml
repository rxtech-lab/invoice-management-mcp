apiVersion: v1
kind: Service
metadata:
  name: invoice-management-service
  namespace: invoice-management
  labels:
    app: invoice-management
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: invoice-management
